# coin-viewer
coin viewer for mar-trader

# Run
$ sudo yarn start

or

$ sudo setcap cap_net_bind_service=+ep `readlink -f \`which node\``

$ yarn start

# Viewer
http://localhost/coin

# Test
http://localhost/test
